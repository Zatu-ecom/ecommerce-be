package common

// response.go — intentionally empty after money/currency standardization.
// All shared contracts (Response, ErrorResponse, BaseListParams,
// PaginationResponse, Money, CurrencyInfo, etc.) now live in common/model.
// Import "ecommerce-be/common/model" directly.
